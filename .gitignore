# Tools, rules, and vulnerability DBs fetched by install.sh (multi-GB, regenerable)
bundle/

# Scan outputs (may contain secret findings / target source paths)
reports/

# Runtime logs
*.log

# Saved NVD API key (secret - never commit)
.nvd_api_key

# Python
__pycache__/
*.pyc
